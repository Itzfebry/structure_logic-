import { AnimatePresence, motion } from "framer-motion";
import { Check, Flower2, HeartCrack, X } from "lucide-react";
import type { RSVPStatus } from "../types/invitation";

interface RSVPModalProps { isOpen: boolean; status: RSVPStatus; onClose: () => void; onSelect: (status: Exclude<RSVPStatus, null>) => void; }

export const RSVPModal = ({ isOpen, status, onClose, onSelect }: RSVPModalProps) => <AnimatePresence>{isOpen && <motion.div className="modal-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose}><motion.div className="rsvp-modal" role="dialog" aria-modal="true" aria-labelledby="rsvp-title" initial={{ opacity: 0, scale: 0.9, y: 20 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: 0.9 }} transition={{ type: "spring", stiffness: 280, damping: 22 }} onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close RSVP dialog"><X size={18} /></button>{status ? <motion.div className="rsvp-success" initial={{ scale: 0.8, opacity: 0 }} animate={{ scale: 1, opacity: 1 }}><div className="success-heart">♡</div><h2>{status === "yes" ? "Yay!" : status === "maybe" ? "So sweet!" : "We understand."}</h2><p>{status === "yes" ? "We can't wait to see you there! 💕" : status === "maybe" ? "That's okay! We hope to see you there 🌸" : "We'll miss you! Thank you for letting us know 💗"}</p><button className="secondary-button" onClick={onClose}>close note</button></motion.div> : <><span className="tiny-label">A LITTLE NOTE BACK</span><h2 id="rsvp-title">Will you come?</h2><p className="modal-copy">Your answer would make our hearts sing.</p><div className="rsvp-options"><button onClick={() => onSelect("yes")}><Check size={18} /><span>Yes, I'll be there</span><b>♡</b></button><button onClick={() => onSelect("maybe")}><Flower2 size={18} /><span>Maybe, let me see</span><b>✦</b></button><button onClick={() => onSelect("no")}><HeartCrack size={18} /><span>Sorry, I can't</span><b>·</b></button></div></>}</motion.div></motion.div>}</AnimatePresence>;
